<div class="absolute top-5 right-5 flex items-center  space-x-2 text-white">
  {{#if isLogged}}
    {{i18n 'Hello'}} {{currentUser}}
    <input type="button" id='logoutbutton' class="cursor-pointer" value=" {{i18n 'logout'}}"></input>
  {{else}}
    <a href='#login'>{{i18n 'Login'}}</a>
  {{/if}}
</div>
