<div class="flex flex-col justify-center items-center h-full w-full text-white">
    <div class="bg-[#323231] rounded-lg p-8 shadow-lg max-w-sm w-full">
        <h2 class="text-3xl font-bold text-white text-center mb-6">{{i18n "Add member"}}</h2>
        <div class="space-y-4 text-[#fff] max-w-md mx-auto" >
            <input type="hidden" name="idProject" value="<?= $project->getIdProject() ?>" />
            <div class="relative flex items-center">
                <input type="email" name="email" placeholder="{{i18n "Email"}}" id="email"
                    class="px-4 py-3 bg-[#323231] w-full text-sm border outline-[#edb705] rounded transition-all"
                    value=""/>
                <div class="absolute right-4 flex items-centerpointer-events-none">
                    <i class="fa-regular fa-envelope text-gray-300"></i>
                </div>
            </div>
            <p class="text-red-600 inlinse">{{#i18n}}{{memberErrors}}{{/i18n}}</p>
            <div class="relative flex items-centerpointer-events-none">
                <button type="submit" name="submit" id="addMember"
                    class="absolute right-0 px-3 py-3 bg-[#edb705] hover:bg-[#ab8403] text-white rounded-lg active:bg-[#006bff]">
                    <i class="fa-solid fa-user-plus"></i>
            </div>

        </div>
        <div class="mt-4 ">
            <a
            href="#view-members?idProject={{selectedProject.idProject}}"
            class="text-yellow-400 hover:text-yellow-500 text-sm underline"
        >{{i18n "Cancel"}}</a>
        </div>
    </div>
</div>